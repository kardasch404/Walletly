<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
    <div class="container">
        <h1>My Budgets</h1>
        <a href="/create-budget">Create New Budget</a>
        <div class="budgets-list">
            <% if (budgets && budgets.length > 0) { %>
                <% budgets.forEach(budget => { %>
                    <div class="budget-item">
                        <h3>Monthly Limit: <%= budget.monthlyLimit %> MAD</h3>
                        <p>Month: <%= budget.mounth %></p>
                        <p>Year: <%= budget.year %></p>
                        <p>Category ID: <%= budget.category_id %></p>
                    </div>
                <% }); %>
            <% } else { %>
                <p>No budgets found. <a href="/create-budget">Create your first budget</a></p>
            <% } %>
        </div>
    </div>
</body>
</html>